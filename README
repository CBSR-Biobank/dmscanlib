How to use scanlib:
-----------------

Decoding Images
====================================
1. Scan the bottom of a pallet at 300, 400, or 600 dpi.

2. Save the file as a BMP. (eg. pallet.bmp)

3. Extract and run scanlib.exe with the following arguments:
	./scanlib.exe -p 1 -d -i pallet.bmp

4. A file scanlib.txt will be created, it will contain the barccodes of each test tube successfully decoded.

5. Debugging files that will be created include: blobRegions.bmp, decoded.bmp, filtered.bmp.

6. For additional command line arguments: ./scanlib.exe --help
